const { Sequelize } = require(".");

/**
 * 예네가 어떤 타입인지에 대한 설명을 적음.
 * visitor 모델을 정의하는 함수
 * @param {import('sequelize').Sequelize} Sequelize
 * @param {import('sequelize').DataTypes} DataTypes
 *   */

const visitor = (Sequelize, DataTypes) => {};
