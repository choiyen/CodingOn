<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form Result</title>
  </head>
  <body>
    <% const path = "./" + file.path %>
    <h1><%= title %> 폼 결과 확인하기</h1>
    <img src= <%= path %> alt="">
    <p><%= userInfo.name %> 님 환영합니다</p>
    당신의 아이디 <%= userInfo.id %> 입니다.</p>
    <p>
    비밀번호 : <%= userInfo.pw %>
    </p>
    <p>
      이름 : <%= userInfo.name %>
    </p>
    <p>
        나이 : <%= userInfo.age %>
    </p>
    <% console.log(userInfo.path); %>
  </body>
  <script>
    window.onload(() => {
      const img = document.querySelector("img");
      img.src = "/" +  file.path;
    })
  </script>
 
</html>
